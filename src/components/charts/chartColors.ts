// color constants for recharts SVG presentation attributes.
// these mirror the CSS custom properties in index.css; update both if values change.
export const chartColors = {
  primary: '#AB47BC',
  primaryMuted: '#CE93D8',
  primaryLight: '#f3e5f5'
}
